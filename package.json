{
  "private": true,
  "scripts": {
    "build": "npm run build --prefix railway-api && npm run build --prefix webapp && npm run typecheck --prefix functions",
    "start": "npm start --prefix railway-api",
    "test": "npm test --prefix railway-api && npm test --prefix functions && npm test --prefix webapp"
  }
}
