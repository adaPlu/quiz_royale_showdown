{
  "name": "quiz-royale-showdown",
  "private": true,
  "workspaces": [
    "backend",
    "webapp"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:webapp": "npm run dev -w webapp",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w webapp",
    "test": "npm run test -w backend"
  }
}
